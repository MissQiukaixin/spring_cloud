server:
  port: 9002


spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        集群化配置
        server-addr: localhost:8848
         # 写虚拟机地址就好了

management:
  endpoints:
    web:
      exposure:
        include: '*'

